
1. page와 레이아웃
  # pages 폴더는 자동으로 주소를 js 파일 이름으로 매핑해준다.
  # pages의 js 이름을 소문자로 해야한다. - 주소 매핑때문에

  # 매개변수의 Type 은 PropTypes를 통해서 검사한다.
  # import PropTypes from "prop-types";
  # PropTypes.node.isRequired 는 매개변수가 node 형태의 필수라는 소리다.

